import { Map } from "immutable";

export const config = Map({
    INDEXED_DB_KEY: 'root',
    INDEXED_DB_STORAGE_NAME: 'stateDB',

    APP_THEMES: {
        LIGHT: 'light',
        DARK: 'dark'
    }
});
